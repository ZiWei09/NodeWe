# NodeWe 竞品与市场判断

版本：v1.0（与当前 NodeWe 实现对齐）  
定位：产品方向和市场进入参考，不是投资或财务预测

## 1. 结论

NodeWe 面向一个明确的交叉需求：让 AI 在客户拥有的服务器、GPU、工作站和实验设备
所在位置执行任务，同时提供企业级身份、权限、审批、审计和撤销能力。市场上相邻方案
通常只覆盖其中一部分：云端沙箱、浏览器自动化、SSH/MCP、私网连接或传统运维治理。

NodeWe 的差异化不应是“另一个远程命令工具”，而应是：

> AI 到达数据和设备所在的位置；节点主动出站，能力显式授权，任务可审计、可撤销，
> 数据不必先复制到云端。

首发客户应优先选择研究机构、GPU/数据团队、机器人与实验设备团队，以及有内网和 VPN
约束的企业研发部门。

## 2. 产品工作定义

用户在自己的服务器或设备上安装 Node Runtime。Agent 只向外建立加密连接；AI Gateway
通过 CLI、SDK、HTTP 或 MCP 适配器提交 Task；Control Plane 负责 Node 身份、路由、
Policy、Approval、Task 生命周期和 Activity 审计。

| 维度 | NodeWe | 相邻方案常见取舍 |
|---|---|---|
| 执行位置 | 客户自有服务器、GPU、设备和 VPN 内资源 | 云沙箱通常在供应商云中执行 |
| AI 接口 | CLI/SDK，并可接入 MCP/API | 传统工具主要提供 SSH、脚本或 Web 控制台 |
| 连接方式 | 节点主动出站 | 远程桌面和 SSH 常要求预先打通入站网络 |
| 治理 | Scope、能力、审批、审计、撤销、租约 | 简单连接工具通常缺少完整责任链 |

## 3. 竞争分层

### 云端代码执行与沙箱

E2B、Daytona、Modal 等产品为 AI 提供隔离、弹性和可复现的云计算环境。它们验证了
AI 执行基础设施的需求，但主要解决“给 AI 一台云中的安全电脑”，不负责访问客户已经
存在的私有节点、内网数据和实验设备。NodeWe 可与这类平台形成互补。

### 浏览器和桌面自动化

Browserbase、Stagehand、Orgo 等产品把浏览器或桌面封装成 API，重点是网页登录、点击、
录制和可观测性。它们的执行机器由平台托管；NodeWe 的执行机器属于客户，重点是文件、
进程、长任务和设备能力。

### 私网连接、身份和 AI 网络治理

Tailscale、Cloudflare 等厂商提供身份驱动的私网连接、策略和 AI/MCP 入口。它们在网络、
身份和企业交付方面具有优势，NodeWe 应运行在这些网络基础设施之上，补足节点能力、
Scope、Task、审批和研究流程治理。

### SSH、MCP 和 AI 运维工具

SSH-MCP、AI VPS 运维工具和自建脚本是最接近的需求验证：AI 可以直接操作真实服务器。
它们通常以单节点、单用户连接为中心，难以统一处理多节点发现、能力声明、最小权限、
一次性注册、长任务、撤销和审计。NodeWe 的控制面正是针对这些治理缺口。

### 传统运维和特权访问

PAM、Rundeck、Ansible 及相关平台在变更控制、批量作业和特权治理上成熟，但通常不是
面向 AI 的 Task 接口，也不负责把自然语言意图安全映射为节点操作。它们是预算替代品和
潜在集成对象，而非完全同构的竞争产品。

## 4. 目标空白位

```text
                         客户自有节点
                    ┌──────────────────────┐
AI 原生、治理完整   │ NodeWe 的目标位置     │
                    │ Node + Task + Policy │
                    └──────────────────────┘
AI 原生、治理较弱   SSH/MCP、AI VPS 工具
传统治理强          PAM、Rundeck、Ansible、网络访问平台
托管执行环境        E2B、Daytona、Modal、Browserbase、Orgo
```

NodeWe 不需要覆盖所有远程执行场景，而要在“客户自有节点 + AI 原生接口 + 研究/企业治理”
这个组合上形成产品心智。

## 5. 首发场景

1. VPN 内的研究服务器只能出网，不能安装完整 AI 工具；
2. GPU 或数据处理节点需要长时间运行、日志回传和结果文件取回；
3. 机器人、实验设备和边缘工作站需要执行脚本或读取状态，但不能暴露公网端口；
4. 企业研发环境要求数据留在内网，每次 AI 操作都可审批、追溯和撤销。

不建议首发进入通用云代码沙箱、通用网页自动化或面向个人的远程电脑市场，这些市场的
产品心智、生态和规模优势已较为集中。

## 6. 进入策略

### 6.1 产品楔子

以“受控节点上的 AI 工作流”而不是“远程 SSH”定位。第一版聚焦四个可量化结果：

- 节点接入时间；
- 从请求到任务开始的延迟；
- 任务成功率和断线恢复率；
- 有完整主体、目标、策略和结果的审计事件比例。

### 6.2 交付方式

- 先提供单组织、单 Control Plane 的私有部署；
- 由平台管理员管理 Grant、Policy、Approval 和外部密钥；
- 通过 CLI/SDK 接入现有 AI 工具，不要求替换客户的 VPN 或 IAM；
- 用 Node Group 和标签覆盖多节点，但所有执行目标都固化为明确 Node ID；
- 以部署模板、预发布手册和审计导出降低试点成本。

### 6.3 可信度建设

安全卖点必须由可验证的工程行为支撑：非 loopback 明文拒绝、mTLS/WSS、Scope 边界、
凭据撤销、单次 Approval、输出限制、加密快照、Activity 哈希链和可回滚升级。任何未
完成的高可用、外部 KMS、IdP 生命周期或 OS 沙箱能力，应明确标注为上线前工作，而不应
出现在当前版本承诺中。

## 7. 市场验证问题

试点访谈应围绕真实工作流，而不是泛泛询问“是否想要 AI 运维”：

1. 哪些节点只能通过 VPN 或跳板机访问？
2. 哪些任务需要长时间运行或跨多个节点？
3. 哪些数据和命令必须审批、脱敏或禁止离开内网？
4. 现有 SSH、脚本、MCP 或 PAM 方案的故障和审计缺口是什么？
5. 谁负责节点凭据、证书、IdP、密钥轮换和审计留存？
6. 试点成功的量化标准是接入时间、任务成功率、人工节省还是合规证据？

## 8. 判断

NodeWe 值得进入预发布试点，但应保持窄产品边界：先把 Node 身份、显式路由、Scope、
Task、Approval 和 Activity 做成可信的基础设施，再按客户场景增加 GPU、仪器、浏览器或
工作流适配器。市场叙事统一围绕“让 AI 在数据和设备所在的位置安全工作”。
