# Optional adapters

This directory is reserved for integrations that are intentionally kept outside
the NodeWe core. An adapter must document its public contract, ownership,
security boundary, dependencies, and test coverage before it is released.

Adapters may translate an external system into NodeWe Tasks, but core NodeWe
types and authorization rules remain authoritative.
